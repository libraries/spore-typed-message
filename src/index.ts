import { accounts } from "./wallet";

console.log(`Main: path on ${__dirname}`)
console.log(`Main: alice.address = ${accounts.alice.address}`)
console.log(`Main:   bob.address = ${accounts.bob.address}`)
